$('#comments', '#content').html("<%= escape_javascript(render :partial => 'comments/comment', :collection => @commentable.comments.reverse, :locals => { :commentable => @commentable }) -%>");
$('#comment-<%= @comment.id -%>-content').effect("highlight", {}, 2000);
