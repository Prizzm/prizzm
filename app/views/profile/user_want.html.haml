#content.grid_15
  #helptool
    %h3 Your profile
    This profile contains all of <em>your data</em>. Everything from the things you have and want to your cases, is placed here for your convenience!
    .close

  %h2 What I want

  %ul.list-display
    - @user.wanted_items.each do |item|
      %li{:id => "item_#{item.id}"}
        = link_to(item) do
          %h2= item.name
          = image_tag item.main_image
          %p= truncate item.review, :length => 110
          Read review
          .cell.delete-item= link_to 'Delete',  [current_user, item],  :remote => true, :method => :delete

