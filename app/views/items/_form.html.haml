-#  
  Using the formtastic form builder gem here. HTML options can be supplied and customized with options on the README page
  http://rdoc.info/github/justinfrench/formtastic/master/file/README.textile or in the original Github repository
  https://github.com/justinfrench/formtastic
= semantic_form_for [current_user, @item] do |form|
  = form.inputs do
    = form.input :name
    = form.input :description
    = form.input :url, :as => :string
    %div{:id => "stars-wrapper-new-item"}
      = form.label 'stars-wrapper-new-item', "Rate"
      = form.collection_select :rating, [1, 2, 3], :to_s, :to_s, {}

  %p= link_to "More options...", '#', :id => 'more-item-options'

  #advanced-new-item
    = form.inputs do
      = form.input :contact_name
      = form.input :email
      = form.input :itemtype 
      = form.input :industry
      = form.input :address
      = form.input :phone_number, :as => :string
      = form.input :secondary_number
      = form.input :facebook
      = form.input :twitter
      = form.input :photo_url, :as => :string

  :javascript
    $("#stars-wrapper-new-item").stars({
      inputType: "select",
      cancelShow: false
    });

  = form.buttons do
    = form.commit_button 

:javascript
  $('#more-item-options').click(function(event){
    $('#advanced-new-item').toggle();
    if ($(this).html() == 'More options...') {
      $(this).html('Less options');
    } else {
      $(this).html('More options...'); 
    }

    return false;
  });
